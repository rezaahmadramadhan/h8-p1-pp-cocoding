<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
    <div class="container">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <form action="/" method="get">
                <button class="brand-btn">
                    <span class="btn-content">
                        <i class="fab fa-cuttlefish"></i>
                        <span>O C O D I N G</span>
                    </span>
                </button>
            </form>

            <style>
                .role-message {
                    background: rgba(255, 245, 245, 0.9);
                    border-radius: 8px;
                    padding: 8px 15px;
                    margin: 0 1rem;
                    border: 1px solid #ffe0e0;
                    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
                }

                .role-highlight {
                    color: #dc3545;
                    font-weight: 700;
                    text-transform: uppercase;
                    letter-spacing: 0.5px;
                }

                .role-message p {
                    font-size: 0.9rem;
                    color: #444;
                    margin: 0;
                    display: flex;
                    align-items: center;
                    gap: 5px;
                }

                .brand-btn {
                    background: linear-gradient(135deg, #6366f1 0%, #a855f7 100%);
                    border: none;
                    border-radius: 50px;
                    padding: 15px 35px;
                    color: white;
                    font-weight: 600;
                    letter-spacing: 1.5px;
                    transition: all 0.3s ease;
                    position: relative;
                    overflow: hidden;
                    box-shadow: 0 4px 15px rgba(99, 102, 241, 0.3);
                    cursor: pointer;
                }
            
                .brand-btn::before {
                    content: '';
                    position: absolute;
                    top: 0;
                    left: -100%;
                    width: 100%;
                    height: 100%;
                    background: linear-gradient(
                        120deg,
                        transparent,
                        rgba(255, 255, 255, 0.2),
                        transparent
                    );
                    transition: 0.5s;
                }
            
                .brand-btn:hover {
                    transform: translateY(-2px);
                    box-shadow: 0 6px 20px rgba(99, 102, 241, 0.4);
                }
            
                .brand-btn:hover::before {
                    left: 100%;
                }
            
                .brand-btn:active {
                    transform: scale(0.98);
                }
            
                .btn-content {
                    display: flex;
                    align-items: center;
                    gap: 12px;
                    position: relative;
                    z-index: 1;
                }
            
                .brand-btn i {
                    font-size: 1.4em;
                    transition: transform 0.3s ease;
                }
            
                .brand-btn:hover i {
                    transform: rotate(360deg);
                }
            </style>

            <% if (error) { %>
                <div class="alert alert-danger mb-0 mx-3 p-2" role="alert">
                    <%= error %>
                </div>
            <% } %>

            <% if (user) { %>
                <div class="role-message">
                    <p class="mb-0">Role kamu: 
                        <span class="role-highlight"><%= user.role %></span>, 
                        semoga bisa lulus Hactiv8 tepat waktu 🚀
                    </p>
                </div>
            <% } %>

            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/courses">Courses</a>
                </li>

                <% if (user) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/profile/<%= user.userId %>/edit">Profile</a>
                    </li>
                    <% if (user.role === "seller") { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/courses/add">Add Courses</a>
                        </li>
                    <% } %>
                <% } %>

                <% if (!user) { %>
                    <li class="nav-item">
                        <a class="btn btn-primary ms-2" href="/login">Login/SignUp</a>
                    </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="btn btn-danger ms-2" href="/logout">Logout</a>
                        </li>
                <% }%>
            </ul>
        </div>
    </div>
</nav>